---
# should we do a version check? (recomended)
submodules_versioncheck: false

# where to download acmetool for debian?
acmetool_fix__deb: 'https://debian.ethz.ch/debian/pool/main/a/acmetool/acmetool_0.2.1-4+b5_amd64.deb'
